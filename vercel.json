{ 
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["routes/**/*", "middleware/**/*", "config/**/*", ".env"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.ts" }
  ]
}
